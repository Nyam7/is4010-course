name: Lab 11 - Rust
on:
  push:
    paths:
      - 'module11/**'
  pull_request:
    paths:
      - 'module11/**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: dtolnay/rust-toolchain@stable
      - name: Run cargo test
        run: cd module11 && cargo test --verbose
      - name: Run cargo fmt check
        run: cd module11 && cargo fmt --check
      - name: Run cargo clippy
        run: cd module11 && cargo clippy -- -D warnings
