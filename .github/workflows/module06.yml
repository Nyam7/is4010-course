name: Lab 06 - Python
on:
  push:
    paths:
      - 'module06/**'
  pull_request:
    paths:
      - 'module06/**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Run Lab 06 tests
        run: pytest module06/tests/ -v
