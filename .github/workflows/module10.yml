name: Lab 10 - Rust
on:
  push:
    paths:
      - 'module10/**'
  pull_request:
    paths:
      - 'module10/**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: dtolnay/rust-toolchain@stable
      - name: Run cargo test
        run: cd module10 && cargo test --verbose
      - name: Run cargo fmt check
        run: cd module10 && cargo fmt --check
      - name: Run cargo clippy
        run: cd module10 && cargo clippy -- -D warnings
