name: Lab 14 - Rust
on:
  push:
    paths:
      - 'module14/**'
  pull_request:
    paths:
      - 'module14/**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: dtolnay/rust-toolchain@stable
      - name: Run cargo test
        run: cd module14 && cargo test --verbose
      - name: Run cargo fmt check
        run: cd module14 && cargo fmt --check
      - name: Run cargo clippy
        run: cd module14 && cargo clippy -- -D warnings
