name: Lab 09 - Rust
on:
  push:
    paths:
      - 'module09/**'
  pull_request:
    paths:
      - 'module09/**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: dtolnay/rust-toolchain@stable
      - name: Run cargo test
        run: cd module09 && cargo test --verbose
      - name: Run cargo fmt check
        run: cd module09 && cargo fmt --check
      - name: Run cargo clippy
        run: cd module09 && cargo clippy -- -D warnings
